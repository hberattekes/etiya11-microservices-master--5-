# Yerel bootstrap ayarları. Asıl konfigürasyon Config Server'dan
# (configs/order-service/*) çekilir.
spring:
  application:
    name: eureka-server
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888
