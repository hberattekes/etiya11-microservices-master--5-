server:
  port: 8888   # Spring Cloud Config'in konvansiyonel portu (gateway 8080'e taşındı)

spring:
  application:
    name: config-server
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hberattekes/etiya11-microservices-master--5-.git
          default-label: main   # repo master dalını kullanıyor (varsayılan 'main' değil)
          search-paths: configs/{application}
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
